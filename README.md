# Requirements

- ag
- js (or any js interpreter, for jslint)
- exuberant ctags
- a solarized terminal
- powerline fonts
- ack

#For installation :
git clone .... .vim
ln -s ~/vim/vimrc ~/.vimrc
cd vim
git submodule init
git submodule update
